# Changelog (Unreleased)

Target release: `v1.2.2`

Comparison baseline: `v1.2.1`

Record image-affecting changes to `manager/`, `worker/`, `copaw/`, `hermes/`, `openclaw-base/`, and `agentteams-controller/` here before the next release.

---

**What's New**

- **Optional AgentTeams Dashboard**: Local installation can deploy the AgentTeams Dashboard for visual Worker, Team, Human, Manager, and Matrix management. Dashboard versioning remains independent from the AgentTeams release. ([#1075](https://github.com/agentscope-ai/AgentTeams/pull/1075), [#1081](https://github.com/agentscope-ai/AgentTeams/pull/1081))
- **Worker push skips non-UTF-8 file names**: `base.sh` gains `is_utf8_name` / `collect_nonutf8_files` helpers (python3-based, safe for invalid byte sequences). The worker change-triggered sync loop and `update-worker-config.sh` build `mc mirror --exclude` lists from them, so a single non-UTF-8 file name no longer fails the whole push; the sync loop also ignores such files so it cannot spin on unpushable ones.

- **SDK first-create detection fix**: `os.ErrNotExist` now passes through the SDK storage driver unwrapped 闁?service-layer `os.IsNotExist` checks (which peel only os-package error types, not arbitrary `Unwrap` chains) can again distinguish "object missing (first create)" from real storage failures, so initialization generate-and-injects missing configs (seeded agent files, SOUL.md, AGENTS.md, openclaw.json merge) instead of aborting. Seed/mirror pushes also skip files with non-UTF-8 names (S3 keys must be valid UTF-8) instead of failing the whole operation.

- **base.sh shipped to all images**: `shared/lib/base.sh` provides `log()`, `waitForService()`, `waitForHTTP()`, and `generateKey()` in every image (manager/worker/controller/copaw/hermes), so `hiclaw-env.sh` consumers get the full `log()` (timestamped with date) instead of the minimal fallback; stale comments claiming base.sh was Manager-only are corrected.

- **MinIO admin API via SDK**: Embedded-mode MinIO user/policy management (`EnsureUser`/`EnsurePolicy`/`DeleteUser` during member provisioning) now follows the `HICLAW_STORAGE_DRIVER` switch instead of always forking `mc admin` subprocesses. `sdk` (default) uses the madmin-go Admin API with the same connection-pooled transport and fast-fail dial timeout as the SDK storage driver; `mc` keeps the legacy `mc admin` CLI path for rollback/parity.

- **S3 SDK storage driver (default)**: The controller's object-storage layer now defaults to the minio-go S3 SDK (`HICLAW_STORAGE_DRIVER=sdk`), replacing the per-call `mc` subprocess fork with a connection-pooled HTTP client 闁?~5.8閼?lower per-member config latency per the `bench_s3` measurements, with static long-lived AK/SK credentials (`HICLAW_FS_ACCESS_KEY`/`HICLAW_FS_SECRET_KEY`) for cloud S3. `HICLAW_STORAGE_DRIVER=mc` restores the legacy driver, and dynamic STS credential sources remain supported on both drivers.

- **Storage stability observability**: New Prometheus metrics expose S3 health and reconcile cost: `hiclaw_storage_op_duration_seconds` (per-op latency histogram, op 閼?driver), `hiclaw_storage_op_errors_total` (op 閼?driver 閼?class: network/timeout/not_found/other), `hiclaw_storage_probe_failures_total`, and `hiclaw_member_reconcile_duration_seconds` (per-member full flow: infra 闁?SA 闁?config 闁?container 闁?expose, kind 閼?result). A rising network/timeout series is the "storage endpoint unstable" alarm that previously showed up only as reconcile stalls.

- **Flaky-storage resilience**: `DeployWorkerConfig` probes storage reachability first (30s bounded, matching the retry window) so a short OSS blip recovers inside the window instead of failing the pass; every SDK operation uses a **2s connect timeout** with transient-failure retries within a **30s total window** (exponential backoff 0.5s闁?s cap; deterministic 4xx/missing-key errors never retried); the mc driver shares the same 30s retry window. A permanently dead endpoint still aborts and requeues with a concise status message instead of stalling on the CLI's 30s dial per op.

- **Package downloads via the SDK driver**: `PackageResolver` now downloads MinIO/OSS packages through the same `StorageClient` (minio-go SDK) instead of forking `mc cp`/`mc stat` subprocesses, which bypassed the driver selection and carried the CLI's own 30s dial timeout 闁?this was the source of `deploy package: package resolve/extract failed: ... mc: unable to prepare URL for copying` under OSS jitter. ETag-based content caching (skip re-download when the etag-named local cache file exists) and atomic temp+rename writes are preserved; `mc` remains only as a nil-storage fallback.

- **Concise Status.Message on reconcile failure**: Storage-layer errors are now single-layer (`storage get <key>: dial tcp ...: i/o timeout`) via `oss.OpError`, and Worker/Manager/Human/Team controllers write a condensed root-cause message (opaque subprocess-exit leaves skipped, 512-char cap) to `status.message` instead of the full multi-layer wrap chain.

- **Worker-management scripts log fallback**: `push-worker-skills.sh`, `generate-worker-config.sh`, and `enable-peer-mentions.sh` now define a defensive `log()` when `hiclaw-env.sh`/`base.sh` did not provide one, so a failed `mc` call logs its `WARNING` properly instead of aborting with a misleading `log: command not found` in images without `base.sh` (controller/worker) or when the shared lib failed to load.

- **Builtin skill push content-compare**: `pushBuiltinSkills` now pushes per-file, skipping unchanged objects (GET-compare instead of a full `mc mirror --overwrite` re-upload), cutting the per-member skill phase from ~25闁?5s to a few seconds while still propagating skill updates from new controller images.

- **Active Team no-requeue mode**: `HICLAW_TEAM_ACTIVE_NO_REQUEUE=true` stops the periodic requeue for fully converged Active Teams whose spec is unchanged 闁?they reconcile only on events (pod phase changes, spec edits) instead of on the 5m timer. Default false preserves the existing periodic behavior.

- **On-demand skill push off by default**: The controller-side local skill push (`spec.skills` via `push-worker-skills.sh`) is skipped by default (`HICLAW_LOCAL_SKILL_PUSH=true` re-enables it) 闁?the script reads the Manager's local `workers-registry.json`, which does not exist in the controller container, so the push always failed there. Remote (nacos) skills still push via Go.

- **Worker CRD session/sandbox metadata fields**: `Worker.spec` gains optional `sessionId` (会话ID) and `sandboxId` (沙箱ID) string properties for associating a worker with an external session or sandbox instance. Both are pass-through metadata: stored on the CR and reflected in the applied-spec hash, with no controller-side behavior changes.

**Bug Fixes**

- **Script `log` fallback**: `hiclaw-env.sh` now defines a minimal `log()` when `base.sh` is absent (controller/worker images), so scripts fail on the real error instead of `log: command not found` (exit 127).

- **QwenPaw-first local install flow**: The installer now presents QwenPaw as the default worker runtime, supports keep-all upgrades with enter-to-keep prompts for existing parameters, and improves non-interactive guardrails for scripted installs.

- **Team human coordinators**: Team resources can include human coordinator members, with team-admin-owned Matrix rooms and updated Team Leader / Worker prompts so coordination stays inside the Team Room.

- **Team Leader coordination refresh**: Team Leader built-ins were refreshed for project planning, DAG task execution, file sharing, communication, organization, mcporter usage, and worker lifecycle coordination. Worker-style anti-loop reply rules were mirrored for Team Leader, and legacy Team Leader skill aliases were removed after migration.

- **CoPaw runtime coordination tools**: CoPaw workers now include runtime hooks and tools for task flow, project flow, messaging, file sync, output sanitizing, credential guarding, health probes, richer readiness handling, and configurable ReAct iteration limits.

- **Nacos remote skills and credentials**: The controller can pass skills API defaults and per-package Nacos authentication to workers, including `authType=nacos|sts-hiclaw|none` and `ai-registry` STS access scope.

- **Worker identity separation**: Controller resource names are separated from runtime worker names across identity, credentials, storage defaults, and readiness reporting, making CR naming and agent-facing names less tightly coupled.

- **Controller observability**: Controller-side reconcile metrics, graceful HTTP/background goroutine shutdown, and test diagnostics were added to make runtime and CI failures easier to inspect.
- **Manager-to-Worker custom Skill delivery**: A Manager can validate, upload, and assign a custom Worker Skill before updating `Worker.spec.skills`; QwenPaw Workers synchronize the assigned Skill into their native workspace and refresh plus enable it without a restart. ([c2d84b09](https://github.com/agentscope-ai/AgentTeams/commit/c2d84b09d3547b258decbb1d24b245c10a98c088), [#1153](https://github.com/agentscope-ai/AgentTeams/pull/1153))

**Bug Fixes**

- **Worker custom Skill loading safeguards**: Grant Managers access to Worker storage prefixes, avoid recursive CR updates during reconciliation, keep local/default MinIO paths off unavailable STS, restore Manager MinIO access after Controller restarts, verify uploaded `SKILL.md` files, and restore Worker file-sync notifications. ([c2d84b09](https://github.com/agentscope-ai/AgentTeams/commit/c2d84b09d3547b258decbb1d24b245c10a98c088), [#1153](https://github.com/agentscope-ai/AgentTeams/pull/1153))
- **QwenPaw Manager custom Skill hot loading**: Continuously mirror workspace Skills into the native QwenPaw workspace, then refresh and enable additions or updates without restarting the Manager. ([c2d84b09](https://github.com/agentscope-ai/AgentTeams/commit/c2d84b09d3547b258decbb1d24b245c10a98c088), [#1153](https://github.com/agentscope-ai/AgentTeams/pull/1153))
- **QwenPaw Manager Skill attachments**: Initialize the Matrix attachment directory at startup so an admin can send a complete Worker Skill ZIP to the Manager for validation and distribution. ([c2d84b09](https://github.com/agentscope-ai/AgentTeams/commit/c2d84b09d3547b258decbb1d24b245c10a98c088), [#1153](https://github.com/agentscope-ai/AgentTeams/pull/1153))
- **Team Room membership convergence**: Explicitly join Team Leaders and Workers with their own Matrix tokens after invitation, so pending invites cannot leave a Team Active while Hermes members remain unreachable. ([c15ba378](https://github.com/agentscope-ai/AgentTeams/commit/c15ba378f9b868778e252bc689e7ea3f5f23a695), [#1142](https://github.com/agentscope-ai/AgentTeams/issues/1142), [#1149](https://github.com/agentscope-ai/AgentTeams/pull/1149))

- **Team reconcile unblocking**: Team reconcile parallelism is configurable via `HICLAW_TEAM_MAX_CONCURRENT_RECONCILES` (default 1, preserving legacy serial behavior; raise it so one slow/hung Team can no longer stall every other Team, including newly created ones stuck in Phase ""/Pending). An optional per-pass deadline (`HICLAW_TEAM_RECONCILE_TIMEOUT_SECONDS`, disabled by default) bounds a single pass against hung external calls; `failTeam` uses exponential backoff (30s 闁?10min cap) and stops requeuing after 5 consecutive failures until re-armed via the `hiclaw.io/retry` annotation; finalizer add/remove use merge patches instead of `Update`. `TeamStatus` gains `observedGeneration`, so unchanged Active teams skip the full provisioning chain after a controller restart or informer re-sync; the periodic requeue for converged teams defaults to 5m, is configurable via `HICLAW_TEAM_RECONCILE_INTERVAL_SECONDS`, and carries 0闁?0% positive jitter so teams do not wake in lockstep.

- **Team reconcile observability**: Each Team reconcile pass logs per-step timing (`team reconcile: admin actor / step 1 rooms / step 1 storage / 闁炽儺娉?, and `DeployWorkerConfig` logs per-phase upload timing, so slow steps (e.g. hung object-storage syncs) are identifiable at a glance. Per-request STS credential INFO logs are commented out (not removed) to reduce log noise; WARN/ERROR paths still log.

- **Team reconcile telemetry**: `Reconcile` injects a team-scoped logger (`team=<name>`, `teamUID=<uid>`) into the context so every downstream layer (deployer, oss, gateway, backend) is tagged with the Team identity 闁?`grep "team=<name>"` covers the whole reconcile span. A unified `timed` helper logs elapsed on success, failure, and cancellation, and member phases now log elapsed on failure too (previously success-only). The oss layer logs `mc slow call` for any mc invocation over 300ms (with op type) to quantify the S3-layer latency distribution; `ProvisionWorker` logs per-step elapsed (Matrix register / MinIO user / room / join / gateway consumer / AI-route auth); `modifyAIRoutes` logs overall elapsed plus 409-conflict retry count; Docker image pulls log completion; a panic guard records reconcile panics with team context and requeues through the error path.

- **S3 benchmark harness**: New `bench/` module with `bench_s3.go` 闁?a read-only reproduction benchmark over the real scenario bucket (mc subprocess vs minio-go SDK drivers, config-phase op mix 12 GET + 3 PUT + 2 STAT + 1 LIST, per-op latency percentiles + per-round wall-clock; write space isolated under `bench-probe/` and auto-cleaned).

- **Debug pprof build switch**: The controller image build accepts `ENABLE_PPROF=true` (`--build-arg`), compiling `cmd/controller` with `-tags pprof` to expose `/debug/pprof` on port 6060 (`HICLAW_PPROF_ADDR` overridable) with block/mutex sampling enabled. Default builds compile a no-op stub 闁?no pprof code, no extra listener, zero debug surface in release images.

- **Human reconcile backoff and parity**: `HumanStatus` gains `observedGeneration` (parity with Worker/Team/Manager) plus `consecutiveFailures` / `maxRetriesReached` / `phaseTransitionTime`. Infra failures now use exponential backoff (30s 闁?10min cap) and stop requeuing after 5 consecutive failures until re-armed via the `hiclaw.io/retry` annotation, replacing the previous double-requeue (`RequeueAfter` + rate-limiter error) pattern.
**閺傛澘顤冮崝鐔诲厴**

- **Manager 閸?Worker 娑撳褰傞懛顏勭暰娑?Skill**閿涙瓉anager 閸欘垰婀弴瀛樻煀 `Worker.spec.skills` 閸撳秵鐗庢灞烩偓浣风瑐娴肩姴鑻熼崚鍡涘帳閼奉亜鐣炬稊?Worker Skill閿涙焙wenPaw Worker 娴兼艾鐨㈠鎻掑瀻闁?Skill 閸氬本顒為崚鏉垮斧閻?workspace閿涘苯鑻熼崷銊︽￥闂団偓闁插秴鎯庨惃鍕剰閸愬吀绗呴崚閿嬫煀閸滃苯鎯庨悽銊ｂ偓?[c2d84b09](https://github.com/agentscope-ai/AgentTeams/commit/c2d84b09d3547b258decbb1d24b245c10a98c088), [#1153](https://github.com/agentscope-ai/AgentTeams/pull/1153))

**Bug 娣囶喖顦?*

- **Worker 閼奉亜鐣炬稊?Skill 閸旂姾娴囨穱婵囧Б**閿涙碍宸挎禍?Manager 鐎?Worker 鐎涙ê鍋嶉崜宥囩磻閻ㄥ嫯顔栭梻顔芥綀闂勬劧绱濋柆鍨帳鐠嬪啫鎷伴張鐔兼？闁帒缍婇弴瀛樻煀 CR閿涘矂浼╅崗宥嗘拱閸︾増鍨ㄦ妯款吇 MinIO 鐠侯垰绶炵拠顖濐嚞濮瑰倷绗夐崣顖滄暏閻?STS閿涘瓔ontroller 闁插秴鎯庨崥搴划婢?Manager 閻?MinIO 鐠佸潡妫堕獮鑸电墡妤犲苯鍑℃稉濠佺炊閻?`SKILL.md`閿涘苯鎮撻弮鑸典划婢?Worker 閻?file-sync 闁氨鐓￠妴?[c2d84b09](https://github.com/agentscope-ai/AgentTeams/commit/c2d84b09d3547b258decbb1d24b245c10a98c088), [#1153](https://github.com/agentscope-ai/AgentTeams/pull/1153))
- **QwenPaw Manager 閼奉亜鐣炬稊?Skill 閻戭厼濮炴潪?*閿涙碍瀵旂紒顓炵殺 workspace Skill 閹舵洖濂栭崚?QwenPaw 閸樼喓鏁?workspace閿涘苯鑻熼懛顏勫З閸掗攱鏌婇妴浣告儙閻劍鏌婃晶鐐村灗閺囧瓨鏌婇惃?Skill閿涘本妫ら棁鈧柌宥呮儙 Manager閵?[c2d84b09](https://github.com/agentscope-ai/AgentTeams/commit/c2d84b09d3547b258decbb1d24b245c10a98c088), [#1153](https://github.com/agentscope-ai/AgentTeams/pull/1153))
- **QwenPaw Manager Skill 闂勫嫪娆?*閿涙艾鎯庨崝銊︽閸掓稑缂?Matrix 闂勫嫪娆㈤惄顔肩秿閿涘奔濞囩粻锛勬倞閸涙ê褰叉禒銉ョ殺鐎瑰本鏆ｉ惃?Worker Skill ZIP 閸欐垿鈧胶绮?Manager閿涘瞼鏁遍崗鑸电墡妤犲苯鑻熼崚鍡楀絺閵?[c2d84b09](https://github.com/agentscope-ai/AgentTeams/commit/c2d84b09d3547b258decbb1d24b245c10a98c088), [#1153](https://github.com/agentscope-ai/AgentTeams/pull/1153))
- **Team Room 閹存劕鎲抽弨鑸垫殐**閿涙岸鍊嬬拠?Team Leader 閸?Worker 閸氬函绱濇担璺ㄦ暏閸氬嫯鍤滈惃?Matrix token 閺勬儳绱￠崝鐘插弳 Team Room閿涘矂浼╅崗?Team 瀹告彃顦╂禍?Active 閻樿埖鈧焦妞?Hermes 閹存劕鎲虫禒宥呬粻閻ｆ瑥婀?invite閵嗕焦妫ゅ▔鏇熷复閺€鑸电Х閹垬鈧?[c15ba378](https://github.com/agentscope-ai/AgentTeams/commit/c15ba378f9b868778e252bc689e7ea3f5f23a695), [#1142](https://github.com/agentscope-ai/AgentTeams/issues/1142), [#1149](https://github.com/agentscope-ai/AgentTeams/pull/1149))
- **Worker and Team display names**: Add an optional `displayName` to Worker and Team specs, synced to the Matrix profile displayname (Worker) and Team room name (Team) behind a generation-guarded idempotency marker (`status.displayNameSyncedGeneration`), and surfaced in the Controller REST API plus `agt` create/update/get (new `--display-name` flag and `DISPLAY-NAME` column).
- **Optional Human display name**: `spec.displayName` on the Human CR is now optional 鈥?empty leaves the Matrix profile displayname unset (no `SetDisplayName` call), the CRD `required` list narrows to `permissionLevel`, and `agt create human` no longer demands `--display-name`.
- **Custom Human initial password**: Add an optional `spec.initialPassword` to the Human CR so callers can pin the Matrix password assigned on first provisioning (`legacy_password` identity source only). When omitted, the controller generates a random password and persists it into **both** `spec.initialPassword` (durable, auditable) and `status.initialPassword` on creation via a one-shot spec write-back; later edits never reset a password the user has rotated. Surfaced in the Controller REST API and `agt create human` (new `--initial-password` flag).
- **QwenPaw 2.0 runtime unification**: Migrate the Manager container from copaw 1.0.2 to QwenPaw 2.0.1 on a single venv, register projectflow/taskflow/message/filesync tools through a QwenPaw plugin instead of monkey-patching CoPawAgent, replace the physical Matrix channel overlay with the QwenPaw plugin system, read Matrix credentials directly from agent.json so the manager tools work without importing copaw at runtime, align CMS observability packages and env vars with the Worker image, inject session-file privacy policy into prompt files, set approval_level=AUTO in the agent template, bridge YOLO mode to Qwenpaw approval_level=OFF, disable the built-in QA Agent, replace start-copaw-manager.sh with start-qwenpaw-manager.sh, add explicit qwenpaw Manager and Worker runtime values alongside copaw while keeping user-facing installer defaults and image pulls on CoPaw until the QwenPaw release, make task assignment state and Matrix notification atomic with room membership validation and m.mentions delivery, preserve m.mentions metadata in streamed/edit events, make the TeamHarness MCP `delegate_task` path atomic too (validate assignee room membership 鈥?strictly `join`, not `invite` 鈥?prepare 鈫?stable-txn notification 鈫?commit assigned + event_id; the initial file publish gates the notification and the assigned/eventId commit gates success, both returning a retryable failure so an idempotent retry finishes the sync instead of reporting success with stale shared storage), and migrate a legacy Worker `.copaw` working dir to `.qwenpaw` on the qwenpaw_worker startup path only (idempotent; the migration follows the target runtime 鈥?an explicitly configured copaw Worker keeps `.copaw` and never migrates before a switch).
- **Custom model capability overrides**: `AGENTTEAMS_MODEL_VISION` and `AGENTTEAMS_MODEL_REASONING` env vars let deployments override vision and reasoning capabilities for custom models not in the built-in presets table (e.g. local multimodal models like `qwen3.6-27b-fp8`).
- **Synapse homeserver support via the MatrixOps abstraction**: Introduce a provider-agnostic `MatrixOps` business layer over the raw Matrix protocol clients, selected by `AGENTTEAMS_MATRIX_PROVIDER` (`tuwunel` default, `synapse`). Room lifecycle, membership, aliases, room metadata, system messages, membership queries (`ListRoomMembers`/`ListJoinedRooms`/`IsUserInRoom`/`IsManagerJoinedDM`), and a homeserver `HealthCheck` are implemented for both homeservers; Synapse write operations automatically recover by classifying the sender-side rejection 鈥?force-joining the acting user via `POST /_synapse/admin/v1/join/{roomID}` when they are no longer in the room, or promoting them via the admin `make_room_admin` API when they lack the required power level 鈥?then retrying the CS operation. The controller's startup `waitForMatrix` now polls through `MatrixOps.HealthCheck` instead of the protocol client.
- **Synapse support documentation**: Add `docs/synapse.md` covering provider switching (`matrix.provider=synapse`), the declarative AppService registration model (Helm-managed `app_service_config_files` vs Tuwunel's runtime admin-bot registration), the namespace security model (`AGENTTEAMS_MATRIX_APPSERVICE_USER_NAMESPACE_REGEX` escape hatch for shared Synapse clusters), token rotation workflows (runtime on Tuwunel, restart-only on Synapse), and the classified force-join / `make_room_admin` recovery paths operators occasionally see in logs.
- **Cross-implementation equivalence suite**: Add `agentteams-controller/internal/matrix/ops_exhaustive_test.go` pinning behavior parity between `TuwunelMatrixOps` and `SynapseMatrixOps` for every `MatrixOps` method 鈥?GROUP A success-path equivalence on the shared CS API surface, GROUP B wire-divergence paths (admin-bot chat on Tuwunel vs Synapse REST, plus the classified force-join / `make_room_admin` sender-recovery fallbacks), and GROUP C AppService governance divergence (declarative on Synapse vs runtime on Tuwunel) with `RenderAppServiceRegistration` struct equality across the nil-vs-set push URL branch.
- **MatrixOps convergence comment/doc maintenance**: Align business-layer comments and docs with the provider-agnostic `MatrixOps` design so homeserver-generic behavior is no longer attributed to Tuwunel. Updates `docs/architecture.md` "Matrix (Tuwunel)" 鈫?"Matrix (Tuwunel / Synapse)" with the `AGENTTEAMS_MATRIX_PROVIDER` selection and `MatrixOps` convergence notes; neutralizes Tuwunel attribution in `provisioner.go` (ManagerEnabled, `ensureMatrixToken` dual-provider reuse rationale), `provisioner_human.go` (JoinRoomAs `trusted_private_chat` is a Matrix spec preset, not a Tuwunel feature), `human_reconcile_delete.go`, `human_reconcile_rooms.go`, `manager_reconcile_welcome.go`, and `humanidentity/identity_source.go`; renames the `initializer` gateway config field `TuwunelURL` 鈫?`MatrixURL` (backed by `MatrixServerURL`, so it already served Synapse in k8s mode) and corrects the gateway route comments (the `/_tuwunel/*` route never existed 鈥?only `/_matrix` is registered; the "tuwunel" service source name is kept as the historical stable identifier).

**Bug Fixes**

- **CoPaw Team assignment handoff**: `taskflow(delegate_task)` sends the Worker assignment automatically with `m.mentions` in the Team Room (atomic pending 鈫?prepared 鈫?assigned state, stable Matrix txn_id for idempotent retries, normalize Worker aliases from the Team roster, refresh Controller-managed runtime context every minute, and reroute assignment replies from non-Team rooms to the Team Room). ([#1120](https://github.com/agentscope-ai/AgentTeams/pull/1120), [#1095](https://github.com/agentscope-ai/AgentTeams/pull/1095))
- **Docker Worker ServiceAccount token rotation**: Project short-lived tokens into per-Worker Docker volumes, refresh the token file atomically without recreating running Workers, and remove the credential volume with the Worker.
- **Worker port exposure CLI**: Encode `--expose` values as numeric ports and reject invalid or out-of-range inputs before create, update, or apply requests reach the Controller.
- **QwenPaw MCP policy startup convergence**: Persist built-in plugin MCP policies before runtime desired-state reloads so a replacement QwenPaw workspace cannot retain the pre-policy interactive approval handler.
- **QwenPaw Team policy and runtime-aware acceptance**: Merge Team and Worker channel-policy overrides into QwenPaw `runtime.yaml`, wait for public plugin/API state before integration assertions, and verify prompt/config files from the runtime location that consumes them.
- **QwenPaw 2.0 tool execution**: Preserve QwenPaw's asynchronous tool-result stream while sanitizing output, and allow only the built-in TeamHarness and Workerflow MCP drivers to run without an unavailable interactive approval prompt.
- **QwenPaw package and Team storage access**: Preserve referenced members' effective Team name across independent Worker reconciles, update MinIO policies without detaching active Workers, revoke access on detach, and grant Workers read-only access to centrally uploaded AgentSpec packages.
- **QwenPaw inline prompt compatibility**: Project Worker identity, SOUL, and AGENTS content through runtime desired state and apply it to both the native QwenPaw workspace and the Worker root storage contract.
- **Integration failure diagnostics**: Export AgentTeams container state and timestamped logs with CI artifacts, classify QwenPaw startup errors without exposing their message contents, and stop the TeamHarness shard after startup failures instead of hiding the cause behind cascading timeouts.
- **QwenPaw local startup readiness**: Select and propagate the QwenPaw Worker image across install backends, then wait for the runtime-owned `runtime/runtime.yaml` object before creating a local Docker Worker.
- **QwenPaw 2.0 runtime compatibility**: Adapt the QwenPaw Worker image, custom Matrix Channel, and native plugins to QwenPaw 2.0.1 startup and schema contracts.
- **Multimodal model image support**: Add `supports_multimodal` and `supports_image` to `agents.defaults` in generated openclaw.json when the selected model supports image input, so QwenPaw does not strip images at the framework layer. Fixes #931.
- **Install script model env passthrough**: Pass custom model override env vars to the Controller container so that `AGENTTEAMS_MODEL_VISION` and related settings actually reach the config generator.
- **Bridge model capability propagation**: Propagate model `input` modalities from openclaw.json through `_write_providers_json()` so QwenPaw's `ModelInfo` receives `supports_image`/`supports_video`/`supports_multimodal` flags instead of relying on fail-open defaults.
- **Manager diagnostic loops**: Manager prompts and Worker lifecycle guidance stop repeated no-op troubleshooting commands and treat a missing Worker in `agt get workers` as the deletion boundary instead of looping on Matrix room probes. ([#975](https://github.com/agentscope-ai/AgentTeams/pull/975))
- **Matrix kick idempotency and ForceLeaveRoom fallback**: Tighten the kick-failure matcher so only the target-not-in-room idempotent case (`"target user is not in"` / `"is not in the room"` / `"not a member"`) returns nil 鈥?previously the matcher also swallowed real `"cannot kick user"` power-insufficient errors as no-ops. The `shouldForceLeaveAfterKickError` escalation matcher now recognizes both Tuwunel-style (`"not have enough power"`) and Synapse 1.127-style (`"cannot kick user"` / `"cannot unban user"` / `"not in room"`) power/sender errors, so `ForceLeaveRoom` and `RemoveMember` route to the correct provider-specific fallback (admin-bot `force-leave-room` on Tuwunel; classified sender recovery on Synapse 鈥?force-join via `POST /_synapse/admin/v1/join/{roomID}` for not-in-room, `make_room_admin` + CS kick retry for power-insufficient) instead of silently dropping the failure.
- **AppService management handler hardcoded TuwunelClient**: Replace the hardcoded `matrix.TuwunelClient` construction inside `appservice_mgmt_handler.go::RotateToken` with a provider-aware path through the injected `MatrixOps` / `matrix.Config`. On Synapse the endpoint now returns HTTP 501 with Helm-guidance JSON (declarative registrations cannot be rotated at runtime); on Tuwunel it preserves the existing runtime rotation flow via `MatrixOps.RegisterAppService`.
- **Synapse room deletion (DissolveRoom) always failed with 400**: `DELETE /_synapse/admin/v2/rooms/{id}` was sent with an empty body, but Synapse 1.127 parses that endpoint's body with `parse_json_object_from_request` (no `allow_empty_body`), returning HTTP 400 `M_NOT_JSON` "Content not JSON." The client now sends an explicit `{}` JSON object body, so `DissolveRoom` works on Synapse; the equivalence test now asserts a JSON body is present.
- **Synapse exclusive AppService users namespace blocked admin creation**: The Helm-rendered AS registration claimed the users namespace with `exclusive: true` over the broad default `@.*:<server>`. Synapse 1.127 rejects *any* other creation of an exclusively-claimed user ID 鈥?including the shared-secret admin bootstrap (`register_new_matrix_user -a`) and the admin API `PUT /_synapse/admin/v2/users/{id}` 鈥?with HTTP 400 `M_EXCLUSIVE` (no admin bypass), which made the admin user and every admin-provisioned user uncreatable on the default config. The users namespace is now `exclusive: false` (AS register/login only need the regex to match), restoring admin bootstrap and admin-API provisioning.
- **Synapse user re-provisioning silently killed device sessions**: `SynapseClient.EnsureUser` now sends `logout_devices: false` on `PUT /_synapse/admin/v2/users/{id}`. Synapse 1.127 defaults `logout_devices` to `true`, so every idempotent re-provision of an existing user (reconcile retries, worker/manager re-apply) invalidated the running device sessions; the flag keeps sessions alive when the same password is re-applied.

---

**闁活喗娼欏鏍箑瑜嶈ぐ澶愬即?/ 闁告娲ㄦ鍥╂嫚鐎涙ɑ顫?*

- **AgentTeams 闁告稖妫勯幃鏇㈠箣閹邦亣绀嬮柛鐑嗗灟缁旀挳鎮介悢鍛婃珡闁汇劌瀚换宥囨偘鐏炵偓顦у┑鍌涘灩鐎?*闁挎稒鑹鹃悾銊ф啑閸涱厽鐝ゅ☉?Helm 闁稿繈鍎辫ぐ娑㈠Υ娑撳攷ntroller 闁告牕鎳岄埀顑挎agt` CLI闁靛棔鑳堕獮鍡樻櫠閸愩劌缍侀梺鎻掔箞閳ь兛娴囩换宥囨偘鐏炵偓顦ч悹渚灠缁剁偤濡存担鐣屻偒婵犙勫姇閹洜绮旈弶鎸庡閻庢稒锚閸嬪秹宕滃鍥╃；鐎规瓕灏欓顒勫礆閹殿噮浼傜紓浣哄枍缁斿瓨绋?AgentTeams闁靛棗鍊瑰Λ?HiClaw wrapper 闁告粌鏈璺ㄦ崉閸愨晝鐖遍柣顔绘閼垫垿鎯冮崟顐㈡倯閻庡湱鎳撻崹搴ㄥ绩椤栨艾鍤掔紒澶婎煼濞呭酣鏁嶅☉妤冪煠闁哄唲鍕伕闁告娲橀崹銊╂嚇濮橆厽鎷遍柛妤€娲ㄦ鍥籍鐠鸿櫣绠戝銈堫嚙閸ㄥ繘骞戦姀鐘茬厒 AgentTeams 闁告稖妫勯幃鏇㈠Υ?[#1063](https://github.com/agentscope-ai/AgentTeams/pull/1063), [#1065](https://github.com/agentscope-ai/AgentTeams/pull/1065))
- **Team 濞?Worker 閻犙冨缁喗鎷呯捄銊︽殢 v1.2 闁哄牃鍋撶紓浣哥墕椤ㄦ牜鐥?*闁挎稒鐡杄am 闂侇偅淇虹换?`spec.workerMembers` 鐎殿喗娲滈弫銈夋偑椤掑倻褰岀紒鐙呯磿閹﹪鎯?Worker CR闁挎稒绋愮粭澶愬礃瀹ュ棙鏆滈柟闀愮閸炴挳鎳?Worker 闁瑰瓨鍔曢幉鎶藉Υ娑旂gistry 閺夆晙鑳朵簺閻犱警鍨扮欢鐐哄矗婵犲倸寰撳〒姘箚缁傚棝鎯冮崟顒侊紜闁煎瓨纰嶅﹢浼村Υ?[#1072](https://github.com/agentscope-ai/AgentTeams/pull/1072))

**闁哄倹婢橀·鍐礉閻旇鍘?*

- **闁告瑯鍨堕埀?AgentTeams Dashboard**闁挎稒纰嶅﹢浼村捶閺夎法鏆旈悷浣告噹瑜版煡鏌堥妸褑顔?AgentTeams Dashboard闁挎稑鐬奸弫銈嗙鎼粹€宠閻熸瑥妫楃€佃尙绮婚敍鍕€?Worker闁靛棔绠慹am闁靛棔绗璾man闁靛棔搴渁nager 闁?Matrix闁挎稒楗盿shboard 闁绘鐗婂﹢鎵磼瑜忛悽缁樼▔?AgentTeams 闁绘鐗婂﹢浼存偑椤掑倻褰岄柕?[#1075](https://github.com/agentscope-ai/AgentTeams/pull/1075), [#1081](https://github.com/agentscope-ai/AgentTeams/pull/1081))
- **Worker 闁规亽鍔戦埀顑挎祰閻戯附娼婚崶顒佸 UTF-8 闁哄倸娲ｅ▎銏ゅ触?*: `base.sh` 闁哄倹婢橀·?`is_utf8_name` / `collect_nonutf8_files` 閺夊牆鎳庢慨顏堝礄閼恒儲娈堕柨娑樻椒ython3 闁哄稄绻濋悰娆撴晬鐏炵瓔鍤犻柡鍐У閺呫儳鈧稒顨夋俊顓犫偓鐟邦槸閸欏繘鏁嶆径鍫氬亾閸婂兌rker 闁告瑦蓱濞茶法鎲撮敃鈧ぐ鍌炲触鐏炵虎鍔勭€甸偊浜為獮鍡涘椽?`update-worker-config.sh` 闁硅鍠楅婵嬪几閸曨垪鍋?`mc mirror --exclude`闁挎稑鑻畷鐔哥▔椤忓牊濮?UTF-8 闁哄倸娲ｅ▎銏ゅ触瀹ュ嫮鐟濋柛鎰Т椤曢亶鎳涚€涙ɑ娈婚柟浣冾潐鐢綊鏌呮担鎼炰杭閻犳劑鍎荤槐閬嶅触鐏炵虎鍔勭€甸偊浜為獮鍡涘触鐏炵偓顦ч煫鍥╂櫕閺嗘劖娼诲▎鎴ｎ潶闁哄倸娲ｅ▎銏ゆ晬瀹€鍕級闁稿繐绉堕埞鏍ㄦ姜椤戦敮鍋?

- **SDK 濡絾鐗楅濂稿礆濞戞绱﹂柛鎺嬪€曢悾鐐┍椤旂⒈妲?*: SDK 閻庢稒锚閸嬪秵銇欓崡鐐残楅悗?`os.ErrNotExist` 濞戞挸绉撮崯鈧柛?`OpError` 闁告牕鎳撻ˉ濠囨晬瀹€鈧ú鍧楀箳閵夆斁鍋撹箛搴ｇ倞闁炽儲鏌￠埀顒佹晝ervice 閻忕偛鍊风欢椋庢導閺嶎偅鐣?`os.IsNotExist`闁挎稑鐗嗚ぐ褔宕?os 闁告牕鎳橀弫濠勬嫚椤栨粏顫﹂柛銊ヮ儍閳ь兛妞掔粭澶愭焻閹烘垹绉哄ù鐘电帛閸?Unwrap闁挎稑顦版禒顔藉緞瀹ュ洦鏅搁柡浣哥墳缁辨繈宕氬┑鍡╂綏闁告牗鐗楀?OSS 闂佹澘绉堕悿鍡涘棘閸ワ附顐藉☉鎾崇Т閻°劑宕烽妸銈囩獥閻炴凹鍋呴婊呮兜椤旇法妲曢柛鎺濆亗鐠愮喐锛冮弽顭戝仹闁告帗绋戠紓鎾绘晬閸垺鏅搁柟瀛樺姈閺佺偤宕楅妷顖滅獥缂佸绉撮悺娆撳棘閸ワ附顐介柕鍡曡OUL.md闁靛棔绔窯ENTS.md闁靛棔鐙梡enclaw.json 闁告艾鐗嗛懟鐔兼晬婢舵稓绀夊☉鎾崇Т閸熲偓闁硅翰鍎甸弫濠冪▔椤撶喐鐒介柨娑欑⊕鐢綊鏌呮担鍓插殸閻犵偐鍓濆鍌滄崉鐎圭姷绠栭梻?UTF-8 闁哄倸娲ｅ▎銏ゅ触瀹ュ洦鐣遍柡鍌氭矗濞嗐垽鏁嶉崷? key 闊洤鎳橀妴蹇涘触閸喓銆?UTF-8闁挎稑顧€缁辨繈鏌嗛崹顔煎赋闁告娲戦柌婊堝锤韫囨梹鐎ù鐘烘硾閹洜鈧絻澹堥崵褔寮€涙ê顥楅柟鎭掑姂閳ь兛绀侀妵鎴犳嫻閵夛絺鍋?

- **base.sh 闂?shared/lib 閺夆晜绋戦崣鍡涘箥閳ь剟寮垫径鎰當闁?*: `shared/lib/base.sh` 闁圭粯鍔掔欢?`log()`闁靛棔姊梬aitForService()`闁靛棔姊梬aitForHTTP()`闁靛棔姊梘enerateKey()`闁挎稑顔卆nager/worker/controller/copaw/hermes 闂傗偓濠婂啫鍓奸柛褍娲ょ€垫﹢宕ラ顒傜`hiclaw-env.sh` 闁汇劌瀚粔椋庢嫻鐟欏嫭鐓欓柤瀛樼濠€浼村箯閸喖鐓傞悗鐟版湰閺?`log()`闁挎稑鐗嗛悽顐﹀籍閵夛附鍩傞柡鍐ㄧ埣濡潡骞嬬粵鍦闁兼澘濂旂粭澶愬礃瀹ュ棙笑闁哄牃鍋撻悘?fallback闁挎稒绋戦幃鎾愁潰閵夈倖鍙忔慨婵撶导缁?"base.sh 濞?Manager 闁? 闁汇劌瀚换鍐籍閼稿灚鏆堥梺鎻掝焾閳?

- **MinIO Admin API 閻?SDK**: embedded MinIO 闁汇劌瀚弫銈夊箣?缂佹稒鐗滈弳鎰不閿涘嫭鍊為柨娑樼墛閸ㄦ岸宕ㄥΟ璺ㄦ閻犲鍔掗懙鎴︽儍?`EnsureUser`/`EnsurePolicy`/`DeleteUser`闁挎稑顦粭澶愬礃瀹ュ懏绁奸悗?fork `mc admin` 閻庢稒鍔樼换妯肩矙鐎ｅ墎绀夐柡鈧柅娑滅閻犺櫣鍠栧▓?`HICLAW_STORAGE_DRIVER` 闁告帒娲﹀畷鏌ユ晬濮濇dk`闁挎稑鐗撶划顖滄媼閵堝繒绀嗛柣?madmin-go Admin API闁挎稑鑻ˇ鏌ユ偨?SDK 閻庢稒锚閸嬪秵銇欓崡鐐残楅柣銊ュ缁绘盯骞掗妷锔炬建濞戞挸楠搁幓鈺呮焻閻旀灚浜奸悹?dial 閻℃帒鎳忓鍌炴晬濞屾c` 濞ｅ洦绻勯弳鈧柛?`mc admin` CLI 閻犱警鍨扮欢鐐寸閵夈倗鈹掗柛銉у仦缁?閻庝絻顫夐惁顕€濡?

- **闁哄牜鍓欏﹢瀵糕偓鐟邦槼椤ュ﹥顪€濡鍚囧ù鍏济崢?QwenPaw**: 閻庣懓顦抽ˉ濠囨嚇濮橆厽鎷遍柣婊勬緲濠€顏呭濡搫甯ラ悘鐐存礈閵?QwenPaw 濞达絾绮堢拹鐔割渶濡鍚?Worker 閺夆晜鍔橀、鎴﹀籍鐠佸湱绀夐柛妤€娲ㄦ鍥籍閼稿灚鏆滈柟?keep-all 闁告粌鑻ú鏍ㄦ姜閿旇法绠介柣锝嗙懃閸戯繝寮垫径濠傛闁轰礁搴滅槐婵嬬嵁鐠哄搫绻侀柛鏍ㄧ墧缁繝妫冮悙鐗堝攭濞存粍甯楄啯鐎殿喖绻嬬粭鍛存儍閸曨垱些閻犲浂鍨辨晶鐣屾偘鐏炶偐绠介柟韬插€戦埀?

- **Team 闁衡偓椤栨稑鐦ù婊嗘鐞氼偊宕¤箛姘辨闁?*: Team 閻犙冨缁噣寮ㄩ娑樼槷濠㈠湱澧楀Σ鎴炵閾忕顫﹂柛妤€绻楅惃鐔煎川濡鐏囬柛娑欙公缁辨紙eam Room 闁?team-admin 鐟滅増甯掗惈姗€鏁嶇仦鍊熷珯闁告艾鏈鐐哄即鐎涙ɑ鐓€ Team Leader / Worker 闁圭粯鍔楅妵姘辨嫚瀹ュ繒绀夌痪顓у枙缁绘岸宕¤箛搴ｇ▕闁衡偓閼稿灚娈愰柛?Team Room 濞戞搩鍘归埀?

- **Team Leader 闁告绻嬬紞鏃堟嚄閽樺顫旈柛鎺楁敱閺?*: Team Leader 闁告劕鎳愰悿鍡涙嚄閽樺顫旈柛銉ヮ嚟缁偅銇勯崷顓熺獥閻熸瑥瀚崹婵嬪Υ娑撳粐G 濞寸姾顕ф慨鐔煎箥瑜戦、鎴﹀Υ娴ｈ鐎ù鐘烘硾閸欌剝绂嶉鐘亾娴ｅ湱鐓涢梺顐ｅ搸閳ь兛鑳剁划宥囩磼閸ャ儮鍋撴稊绀硃orter 濞达綀娉曢弫銈夊椽?Worker 闁汇垻鍠庨幊锟犲川閵婏附鍩傞柛妤€绻嬬紞鏃堟煂瀹ュ棙鐓€闁轰礁顕幃濠囨晬濞戞ɑ鍊辨慨?Worker 闁?anti-loop 闁搞儳鍋涢ˇ鑼喆閸曨偄鐏熼柨娑欑缁鸿偐绮旂拠鑼殮闁瑰瓨鍔曢幃妤冪矓婵犳碍鐝熷ù婊冩濡偊鎯?Team Leader 闁瑰灈鍋撻柤瀹犳閸╁棝宕ュ鍐ｅ亾?

- **CoPaw 閺夆晜鍔橀、鎴﹀籍鐠哄搫绀傚ù锝嗙矊娴兼劙宕?*: CoPaw Worker 闁哄倹婢橀·鍐╃鐠囨彃顫ゆ繛缈犻檷閳ь兛绶氶妴宥夋儎椤旂晫銈﹂柕鍡曠劍缁夌兘骞侀妯峰亾娴ｈ鐎ù鐘烘硾閹挸顫㈤妷锝傚亾娴ｇ晫缈婚柛鎴犲劋缁旇煤濡炲皷鍋撴担绋挎闁硅鍠曠换姘跺箮閵堝啠鍋撴担闀愭樊閹兼挳鏀辩敮浼存煢閸稈鍋撴担瑙勭函閻庣懓鏈弳锝夋儍閸曨偅鐨戠紓渚囦簼椤ュ懘寮婚妷褎绁查柛?hooks / tools闁挎稑鑻懟鐔煎绩椤栨稑鐦梺鏉跨Ф閻?ReAct 闁哄牃鍋撳鍫嗗棗鍤″ù鐙呯稻椤愬ジ寮懜顑藉亾?

- **Nacos 閺夆晜绮庨埢濂稿箮閳ь剟鎳楅幋鎺旂憿闁告埈鍘藉畵?*: 闁硅矇鍐ㄧ厬闁革絻鍔岃ぐ鏌ュ触?Worker 濞磋偐濞€閳?skills API 濮掓稒顭堥濠氬磹閻撳孩瀚叉慨锝呯箣闁叉粓宕犻崨顖涚暠 Nacos 閻犱降鍊涢惁澶愭煀瀹ュ洨鏋傞柨娑樻湰閺侇噣骞?`authType=nacos|sts-hiclaw|none` 濞寸姰鍎卞?`ai-registry` STS 闁哄鍟村娲嚑閸愩劍绾柕?

- **Worker 闂婎剦鍋傞崬銈囨喆閿濆牃鍋?*: 闁硅矇鍐ㄧ厬闁革絻鍔忕粊顐⑩攦閹邦剚鍊冲☉鎾虫唉缁诲秶鎮扮仦鐐槯 Worker 闁告艾绉惰ⅷ闁革负鍔忛棅鈺傜婵劏鍋撴担绋挎闁硅鍠撻埀顑跨閻°劑宕掗妸鈺冨笡閻犱降鍊曢埀顒傚帶閹锋壆浜告潏鈺佸磿闁绘鍩栭埀顑挎閼垫垹鎲撮敐鍫氬亾閿旇偐绀夐梻鍕С缂?CR 闁告艾绉惰ⅷ濞?Agent 閻庣數鎳撻ˇ濠氬触瀹ュ泦鐐烘儍閸曨喒鍋撻敃鈧幃搴ㄥΥ?

- **闁硅矇鍐ㄧ厬闁革絻鍔岃ぐ鑼喆閸屾稓銈撮柟?*: 濠⒀呭仜婵偤骞掕閸╂宕?reconcile 闁圭娲﹂悥锝夊Υ娑撶嚟TP 闁哄牆绉存慨鐔哥▔鎼粹剝鍊甸柛?goroutine 闁汇劌瀚槐顓㈡⒖閸涚补鍋撻埀顒勫礄閻氬绀夊ù鐘劚瀵嘲霉鐎ｎ厾妲稿鎯扮簿鐟欙妇鎷犳繝鍐╃劷濞ｅ洠鍓濇导鍛存晬鐏炶偐鈹掑ù婊冨鐢捇寮婚妷銊х閻炴稑鏈鍌炲椽?CI 闂傚偆鍣ｉ。浠嬪Υ?

**Bug 濞ｅ浂鍠栭ˇ?*

- **Worker 閻庢稒锚閸嬪秹宕ョ仦缁㈠妱 I/O 闁衡偓閹佷海**闁挎稒鑹鹃悢鈧ù婊冨閸ㄦ岸宕?watermark 闁告瑯浜欑粭鍌涘閻樻彃缍侀柛鏍ㄧ墬閺嬪啯绂掔拋鍦濞ｅ洦绻冪€?jq 1.7 fallback pull 閻庢稒蓱濡炲潡鏁嶇仦鍊熷珯閻?embedded Controller mirror 闂傚嫭鍔曢悾鐐▔閻戞ê浠橀柛鎺戠埣濞间即鏌婂鍥╂瀭闁靛棗鍊搁懟鐔煎矗閹存繂鐏＄€?Worker 闁告粌鏈﹢顓㈡儗閵夈儰绱ｅù锝嗙矌濞叉媽銇愰弴姘辩煗濞ｅ洦绻冪€垫棃宕㈤悢鍛婄畳闁归晲妞掔粻娆撳礌閺嶎剦鍤斿☉鏂款檧缁辨繃绋夊鍛櫃闁告瑥绉撮ˇ鏌ュ箥瑜戦、鎴﹀礂閵娾晛娅?workspace mirror闁?[#1110](https://github.com/agentscope-ai/AgentTeams/pull/1110))
- **CoPaw Team 閻犱警鍨抽弫杈ㄧ▔?workspace 闁硅埖娲栨總?*闁挎稒鑹鹃惃?Team Leader 闁告帒妫濋崢銈夋晬閸繂鐦堕柟?localpart mention闁挎稑顦抽惌楣冩偨閸楃偛鐓?Team Room闁挎稑鑻懟鐔煎箮?Worker prompt闁靛棔澶焝ills闁靛棔绀佹导鎰板礂閻戣棄甯崇紓鍐惧枛閹?Matrix 閻犱礁澧介悿鍡涘箮閺囩偛顨涢柛?CoPaw 濮掓稒顭堥?workspace闁?[#1060](https://github.com/agentscope-ai/AgentTeams/pull/1060), [9074def](https://github.com/agentscope-ai/AgentTeams/commit/9074def3), [973e291](https://github.com/agentscope-ai/AgentTeams/commit/973e291), [92c8145](https://github.com/agentscope-ai/AgentTeams/commit/92c8145))
- **Team Room 濞?Worker 闁汇垻鍠庨幊锟犲川閵婏附鍩傞柡鈧懜鍨異**闁挎稒鐭换姘跺箮閵堝牜娼剁€殿喗娲滈弫銈夋儍?Worker CR闁挎稑鑻閬嶅礆閼哥數澧″Δ?Team 闊洤鎳庨敐鐐垫喆閹烘洖顥忛柨娑樿嫰閻?Manager 缂佸顕ч崵顓㈠疾椤曗偓閳?Team Worker 闁汇劌瀚柌婊勭閻戞ê鐓ｉ梻鍌濇彧缁辨繈鐛捄鐑樿含 Worker 缂佸倽顕х槐?Team 闁告艾瀛╂禒顔藉緞?standalone 闁瑰瓨鍔曢幉鎶藉礂瀹曞洭鍏囬柕?[d96f1ed](https://github.com/agentscope-ai/AgentTeams/commit/d96f1ed), [43545c2](https://github.com/agentscope-ai/AgentTeams/commit/43545c2), [b5b0add](https://github.com/agentscope-ai/AgentTeams/commit/b5b0add), [a5d6435](https://github.com/agentscope-ai/AgentTeams/commit/a5d6435))
- **v1.2 濞戞柨顑呮晶鐘绘⒐濠婂啫鍓奸柣銊ュ閻ｃ劎鎲楅崨顓炴倯閻?*闁挎稒顒?.1.2 闂傗偓濠婂啫鍓煎ù锝堟硶閺併倝寮楠炲棙鏅堕崘銊ョ秮闂佹彃绻愰〃鏍棯閿曗偓閹锋壆鈧稒锚閸嬪秹宕滃鍥╃；闁挎稑鐦?.2.0 闁告瑥锕ュú鍧楀棘娴煎瓨娈旈柛宥呯箣婵炲洭鎮?AgentTeams 濠靛倹鍨圭€规娊鏁嶅▽?.2.0.beta.1` 缂佹稑顦抽崵婊呪偓瑙勭煯缁犵喐娼忛幘鍐插汲濞村吋淇洪～澶愭嚑閸愩劌顕у☉鎾虫惈閸戯繝宕ｉ幋婵堫伌闁?Tag 闁哄秶鍘х槐锟犲Υ?[#1079](https://github.com/agentscope-ai/AgentTeams/pull/1079), [#1100](https://github.com/agentscope-ai/AgentTeams/pull/1100))
- **Dashboard 閻庣懓顦抽ˉ濠囧矗椤栫偞娴嗛柟?*闁挎稒鐭换姘舵偩?quick-start 濞?keep-all 閻炴稑濂旂拹鐔兼晬鐏炶棄纾崇紒鐙欏嫭顦ч柟顓滃灩椤?Worker 闁告埈鍘藉畵渚€鏁嶇仦鐐毉閺夆晜绋撶紞澶愬礂閾忣偄璧存繛鏉戭儎缁楀瞼鈧懓顦抽ˉ濠冾殽瀹€鍐闁挎稑鑻懟鐔煎捶閵娿儱绁婚弶鐐跺Г濡炲倸銆掗崨顖涘€?Dashboard 闁轰胶澧楀畵渚€濡?[#1081](https://github.com/agentscope-ai/AgentTeams/pull/1081))
- **鐎规悶鍎遍崣鎸庣▔鎼淬倗妲氶柡鍌ゅ幖閻ｃ劑宕楅妸锔瑰亾?*闁挎稒纰嶇€氬棛绱掑┑鍛憹閻庣懓顦崣蹇涙儍閸曨剙绲诲ù鐘烘硾缂嶅﹤顩奸敐澶嬫嚑闁规亽鍎荤槐婵堚偓鐟版湰閺嗭綁鎳樻潏銊︽珱閻犲鍟抽惁顖炲礌閸涱剝鍘柣?Matrix 濞存粌顑勫▎銏ゆ晬瀹€鈧弫鎾诲箣閹邦剙璁查柣鈺佺摠鐢瓨娼婚幇顖ｆ斀闁?Worker ZIP 閻庣數鍘ч崣鍡涘川閹存帗濮㈤柨娑樼焷閻︽垿宕氶銏㈢Ъ闁?OpenClaw cron 闁哄秶鍘х槐锟犳晬鐏炴儳绀嬮柤鎯у槻瀹撳棝寮?replay 闁搞儳鍋涢ˇ鏌ユ晬鐏炲€熷珯闁归晲妞掔粻娆撳礌閺嵮屽晣闁革絻鍔岀€?skopeo 闁汇劌瀚鑽ゆ嫚娴ｈ渹绻嗛柟顓у灛閳?[#1043](https://github.com/agentscope-ai/AgentTeams/pull/1043), [#1045](https://github.com/agentscope-ai/AgentTeams/pull/1045), [#1047](https://github.com/agentscope-ai/AgentTeams/pull/1047), [#1048](https://github.com/agentscope-ai/AgentTeams/pull/1048), [#1049](https://github.com/agentscope-ai/AgentTeams/pull/1049), [#1050](https://github.com/agentscope-ai/AgentTeams/pull/1050))

- **Team 閻犲鍟抽惃顓犳喆閿濆鐝熼梻鍐嚙椤?*: Team 閻犲鍟抽惃顓㈢嵁鐠哄搫绲洪幖杈剧畱瑜版煡鏌呭宕囩畺 `HICLAW_TEAM_MAX_CONCURRENT_RECONCILES` 闂佹澘绉堕悿鍡涙晬閸儳甯涢悹?1闁挎稑濂旂换姘跺箰娴ｇ鏂ч柡鍫濐槷鐟曞棛鎮板畝鍐炬斀濞戞挾灏ㄧ槐杈╂嫬閸愵喚褰柛姘瀹曠喐绋?Team 缂傚倹鎸婚崣鍐箣閺嶃劌鐦悹褑娓圭粭澶愬礃瀹ュ棗鐝涢柛顭戝枛閸欑偓绂掗弽銊ヮ暡闁?Team闁挎稑鑻幆鍫ュ棘閺夎法绱﹂柕鍡曠娴犵娀宕?Phase ""/Pending 闁汇劌瀚哥槐姘跺Υ閸屾艾璁查梺顐ｄ亢缁?`HICLAW_TEAM_RECONCILE_TIMEOUT_SECONDS` 鐎殿喒鍋撻柛姘煎灠瀹曠喎鈻庨檱閻ㄧ喓鎷幇顖溞㈤柡鍐啇缁辨瑦顪€濡鍚囬柛蹇斿▕濡挳鏁嶇仦鑲╃闁归晲绀佺敮顐﹀嫉婢跺寒鏀藉☉鎾跺皑缁辨岸鏁嶅▽姊朼ilTeam` 闁衡偓闁稖绀嬮柟绋挎处閺嗙喖鏌呴埀顒勬焼閸栵紕绀?0s 闁?10min 閻忓繋绶氶妴濠囨晬婢舵稓绀夐弶鈺冨仧閻㈢粯寰勬潏顐バ?5 婵炲棌鈧櫕鍊甸柛瀣矋椤掓盯鎳涢鍕楅梺鎻掔Х閻︻垶鏁嶅畝鍕ㄥ亾濮樺磭绠?`hiclaw.io/retry` 婵炲鍔忚闂佹彃绉甸弻濠囧触椤栨粍鏆忛柨娑欑煠inalizer 濠⒀呭仜閸ㄥ綊寮ㄩ崷顓熸殢 merge patch 闁兼澘鐭傚?`Update`闁靛棔绻朤eamStatus` 闁哄倹婢橀·?`observedGeneration`闁挎稑鏈敮鍫曞礆鐠虹儤鐝ら梺鎻掔Т閹酣骞?informer re-sync 闁告艾瀛╁﹢顓㈠矗濡粯绾柣?Active Team 閻犲搫鐤囩换鍐礂閵娾晛娅ら悹瀣暢閻參鏌ч幘鍛耿鐎圭寮堕弫褰掑极?Team 闁汇劌瀚幊鍡涘嫉?requeue 濮掓稒顭堥?5min闁挎稑鑻ぐ鏌ユ焻濮樺磭绠?`HICLAW_TEAM_RECONCILE_INTERVAL_SECONDS` 闂佹澘绉堕悿鍡涙晬鐏炲€熷珯閻?0闁?0% 婵繐绲介幃婊堝箮閺嵮冃楅梺顒€鐏濋崢銈夊触?Team 闁告艾鏈鍌炲船閵堝鏅柕?

- **Team 閻犲鍟抽惃顓㈠矗椤栨繍娼庢繛鏉戭儐閳?*: Team 閻犲鍟抽惃顓炐掕箛鏃戝仹闁圭瑳鍡╂斀闁圭顦伴鐐搭殽閵堝棗鈪甸柛妤佸閳ь剚顨嗗鍌炲籍閵夈儳绠堕柨娑樻綁team reconcile: admin actor / step 1 rooms / step 1 storage / 闁炽儺娉﹂柨娑橆檧缁辨紮DeployWorkerConfig` 闁瑰灚鎸稿畵鍐触閸曨亞鐟愬ù鑲╁█濡礁鈻撴担鍐ｅ亾濡や焦顦ч柨娑樺缁岃埖绂嶆惔鈩冨渐闂侇偆鍠庨悾鐐媴瀹ュ棗寮慨婵勫劦椤庡啴鏁嶉崼婵愭搐閻庣數顢婇挅鍕偓娑櫭崑宥夊触鐏炵虎鍔勯柟绋垮€介幑锝夋晬婢跺牃鍋撴穱淇哠 闁告埈鍘藉畵渚€鏌呴幇顖ｅ殲婵?INFO 闁哄啨鍎辩换鏃堝绩闁稖绀嬫繛澶堝姂閸ｅ瓨绌卞┑鍫熸畬闁挎稑鐗呯粭澶愬礆閻樼粯鐝熼柨娑橆槷娴滄帡姊藉鍕У闁哄啨鍎辩换鏃堝闯椤忓嫸绱ｉ柨娑欑焻ARN/ERROR 閻犱警鍨扮欢鐐寸瀹ュ嫮绐楅柟鍨尭瀹撳啴濡?

- **Team 閻犲鍟抽惃顓㈡焼閵夛妇銈村褏鍋涘?*: `Reconcile` 闁稿繈鍎辫ぐ娑氫焊?team-scoped logger闁挎稑娼塼eam=<name>`闁靛棔姊梩eamUID=<uid>`闁挎稑顦伴弫鐐哄礂?context闁挎稑濂旂粭鍛€掔粙鎸庡€楅悘鐐插亰缁辨獓eployer/oss/gateway/backend闁挎稑顦板Λ鈺勭疀濡ゅ嫬娈伴柛鏂诲妽閹紕鏁?Team 闂婎剦鍋傞崬銈夋晬鐎瑰碃rep "team=<name>"` 闁告鍟胯ぐ鑼啺閸℃瑦纾伴柡浣哥摠濞碱垳鎷崘顓犳瘔闂佺偓宕橀惌楣冨Υ閸屾稒鐓€濠⒀呭仧缁儤绋夐埀?`timed` helper闁挎稑鏈崹姘跺礉?濠㈡儼绮剧憴?闁告瑦鐗楃粔鐑藉锤閸ヮ亶鍞剁憸?elapsed闁挎稒椹砮mber 闁告艾瀚板Ο浣糕枔闂堟稏浜奸悹鎰╁劥閻儳顕ラ崟顔㈠绋?elapsed闁挎稑鐗嗙敮顐﹀礂閸粎鐭岄柟瀛樺姇婵盯寮懜闈涒叺闁告搴滅槐姘跺Υ娓氱祳S 閻忕偛鍊搁顔炬惥閸涙壆绠?300ms 闁?mc 閻犲鍟伴弫銈夊箥?`mc slow call`闁挎稑鐗嗛幆?op 缂侇偉顕ч悗鐑芥晬婢舵稓绀夐梺鎻掔箰鐎?S3 閻忕偛鍊稿畷鐔封枎闄囬惃鐔兼偨閵娿儲顐介弶鈺冨枎閸ㄥ海鏁崘璺ㄥ耿`ProvisionWorker` 闁告艾瀚鐐搭殽閵堝繒绀凪atrix 婵炲鍔岄崬?/ MinIO 闁活潿鍔嶉崺?/ 鐎点倗鍎ら崺?/ join / gateway consumer / AI 閻犱警鍨抽弫閬嶅箳閸喐缍€闁挎稑顦宠棢 elapsed闁挎稒鐭杕odifyAIRoutes` 閻犱焦婢樼紞宥夊极缂堢姷绉奸柤鐗堫殕濡炲倹绋?409 闁告劘灏欓悰濠囨煂瀹ュ牏妲告繛鍡忓墲閺嗙喖鏁嶅Ч顧礳ker 闂傗偓濠婂啫鍓奸柟宄邦槸瑜板洨鈧懓鏈崹姘辨偘閵夛附锛夐煫鍥殣缁遍亶寮弶娆炬澔 panic 闁稿繑绮岀花鎶芥晬鐎圭瀽nic 闁哄啳娉涢悽?team 濞戞挸锕ｇ粭鍛村棘閸ヮ亶鍞剁憸鐗堟礀閼荤喓鎸ф导瀛樻櫓閻犲浂鍨甸惌鎯ь嚗?requeue闁?

- **S3 闁糕晛鎼崳顖涘緞瀹ュ洤绠涚€规悶鍎遍崣?*: 闁哄倹婢橀·?`bench/` module闁挎稑鑻幆?`bench_s3.go` 闁炽儲鏌￠埀?濠㈣泛绉堕弫銈夋儑閻旈鏉介柛锔惧劋濞呮瑥顩奸崜浣圭暠闁告瑯浜ｉ鐗堝緞瀹ュ洤绠涢柛鈺佹惈閸ｎ垶鏁嶉崸顣?閻庢稒鍔樼换妯肩矙?vs minio-go SDK 闁告瑥鐭傞埞宥夊礉椤帞绀夐悗闈涚秺缂?config 闂傚啳鍩栭宀勫箼瀹ュ嫮绋婇梺鏉跨У閻?12 GET + 3 PUT + 2 STAT + 1 LIST闁挎稑鐭佺欢顓㈠礄閸濆嫭鍊楅柟鍨С缂嶆柨顕欓幆鎵闁告帒妫旂紞鍛村极妫颁胶鐟㈤柛妤佹礃閸ㄦ岸宕ㄥΟ璺ㄦ瀭婵炲棌鈧剚鏆伴梺鐣屽枙閳ь剚顨嗗鍌炴晬濞戞ê鏅哥紒灞炬そ濡潡姊鹃弮鍌ょ€查柛?`bench-probe/` 闁告挸绉剁槐鎴炵▔鐎ｎ亣瀚欓柤濂変簻婵晛銆掗崨顖涘€為柨娑橆槶閳?

- **pprof 閻犲鍟抽惁顖炲几閸曨偆绱︾€殿喒鍋撻柛?*: 闁硅矇鍐ㄧ厬闁革絻鍔戦弳鍛村磽韫囨梻鈧垰顕欓悜妯绘殰闁?`ENABLE_PPROF=true`闁挎稑娼?-build-arg`闁挎稑顧€缁辨繃绂?`-tags pprof` 缂傚倹鐗為惁?`cmd/controller`闁挎稑鏈В姘舵?6060 缂佹棏鍨拌ぐ?`/debug/pprof`闁挎稑鐗嗚ぐ鏌ユ偨?`HICLAW_PPROF_ADDR` 閻熸洖妫涘ú濠囨晬婢跺﹨瀚欑€殿喒鍋撻柛?block/mutex 闂佹彃娲﹂悧閬嶅Υ閸岀偟甯涢悹浣靛€栭悗顖氼嚈閾忓湱妞介悹?no-op stub闁炽儲鏌￠埀顒佹煣缁楀宕?pprof 濞寸媴绲块悥婊堝Υ娴ｉ鐟濈€殿喒鍋撳Λ鐗堢箓椤﹁崵绮╅姘稉闁挎稑鑻ぐ鍌滄暜閸愵喗娈旈柛宥呯箻濞村倻鎷崘顓犳Ц闂傚牜娼块埀?

- **Human 閻犲鍟抽惃顓㈡焻閳ь剟鏌嗘径鍝ョ憿閻庢稒顨嗛宀€鈧潧缍婄紞?*: `HumanStatus` 闁哄倹婢橀·?`observedGeneration`闁挎稑鐗呯粭?Worker/Team/Manager 閻庨潧缍婄紞鍫ユ晬婢跺绨伴柛?`consecutiveFailures` / `maxRetriesReached` / `phaseTransitionTime`闁靛棔鐥塶fra 濠㈡儼绮剧憴锕傚绩闁稖绀嬮柟绋挎处閺嗙喖鏌呴埀顒勬焼閸栵紕绀?0s 闁?10min 閻忓繋绶氶妴濠囨晬婢舵稓绀夐弶鈺冨仧閻㈢粯寰勬潏顐バ?5 婵炲棌鈧櫕鍊甸柛瀣矋椤掓盯鎳涢鍕楅梺鎻掔Х閻︻垶鏁嶅畝鍕ㄥ亾濮樺磭绠?`hiclaw.io/retry` 婵炲鍔忚闂佹彃绉甸弻濠囧触椤栨粍鏆忛柨娑欑◥閹便劍寰勫鍕晩闁告鍠庨崢?`RequeueAfter` + error 闁汇劌瀚濠氭煂?requeue 婵☆垪鈧磭纭€闁?

---

**Change list / 闁告瑦蓱濞插潡宕氬Δ鍕┾偓?*
- fix(controller): pass os.ErrNotExist through the SDK storage driver unwrapped 闁?restores os.IsNotExist first-create detection (generate-and-inject) that OpError wrapping had broken; skip non-UTF-8 file names in seed/mirror pushes ([9393e72](https://github.com/agentscope-ai/HiClaw/commit/9393e72))
- feat(shared): skip non-UTF-8 file names in mc mirror pushes 闁?base.sh is_utf8_name/collect_nonutf8_files helpers; worker-entrypoint.sh sync loop and update-worker-config.sh exclude them so one bad name cannot fail the whole push ([185a54e](https://github.com/agentscope-ai/HiClaw/commit/185a54e))
- feat(shared): ship base.sh in all images via shared/lib 闁?full log()/waitForService/waitForHTTP/generateKey, stale Manager-only comments corrected ([b1d31bb](https://github.com/agentscope-ai/HiClaw/commit/b1d31bb))
- feat(controller): expose storage connect/retry/probe tuning as `HICLAW_STORAGE_*` env vars 闁?connect timeout (`HICLAW_STORAGE_CONNECT_TIMEOUT_SECONDS`), retry window (`HICLAW_STORAGE_RETRY_WINDOW_SECONDS`), backoff base/cap (`HICLAW_STORAGE_RETRY_BACKOFF_MS` / `_MAX_MS`), SDK internal retries (`HICLAW_STORAGE_SDK_MAX_RETRIES`), config-phase probe (`HICLAW_STORAGE_PROBE_TIMEOUT_SECONDS`); defaults unchanged (2s / 30s / 500ms闁?s / 2 / 30s) ([7898244](https://github.com/agentscope-ai/HiClaw/commit/7898244))
- feat(controller): add madmin-go admin provider for embedded MinIO user/policy management, following the HICLAW_STORAGE_DRIVER switch (sdk default; mc admin CLI kept as legacy provider) ([7898244](https://github.com/agentscope-ai/HiClaw/commit/7898244))
- feat(controller): add minio-go S3 SDK storage driver 闁?HICLAW_STORAGE_DRIVER=sdk default with mc fallback, flaky-storage resilience (probe fast-abort, bounded retries), content-compare builtin skill push ([95dcae1](https://github.com/agentscope-ai/HiClaw/commit/95dcae1))
- feat(controller): optional no-requeue for converged Active teams 闁?HICLAW_TEAM_ACTIVE_NO_REQUEUE, plus team member reconcile flow metrics ([84a9429](https://github.com/agentscope-ai/HiClaw/commit/84a9429))
- fix(shared): add log fallback when base.sh is absent 闁?scripts fail on the real error instead of exit 127 ([c895d3a](https://github.com/agentscope-ai/HiClaw/commit/c895d3a))
- feat(controller): add storage stability and member reconcile metrics 闁?storage op duration/errors (op x driver x class), probe failures, member flow duration ([77ef4b8](https://github.com/agentscope-ai/HiClaw/commit/77ef4b8))
- feat(controller): add team-scoped reconcile telemetry 闁?ctx logger injection (team/teamUID), timed helper, failure-path elapsed, mc slow-call threshold logs, ProvisionWorker/modifyAIRoutes/ensureImage timing, panic guard ([ce1a531](https://github.com/agentscope-ai/HiClaw/commit/ce1a531))
- test(bench): add S3 reproduction benchmark under bench/ (mc vs minio-go SDK, real-bucket read-only) ([a12c5b3](https://github.com/agentscope-ai/HiClaw/commit/a12c5b3))
- feat(controller): add build-time pprof switch 闁?ENABLE_PPROF build arg with -tags pprof, no-op stub in default builds ([6cf6f86](https://github.com/agentscope-ai/HiClaw/commit/6cf6f86))
- feat(controller): make Active Team reconcile interval configurable with positive jitter ([462f84d](https://github.com/agentscope-ai/HiClaw/commit/462f84d))
- fix(controller): build hiclaw-controller image with shared/lib via named build context ([82a75e8](https://github.com/agentscope-ai/HiClaw/commit/82a75e8))
- feat(controller): add per-step Team reconcile timing logs, configurable max concurrency, and quieter STS INFO logs ([c01aaec](https://github.com/agentscope-ai/HiClaw/commit/c01aaec))
- fix(controller): unblock Team reconcile 闁?concurrency, failTeam backoff, observedGeneration fast path ([48ce4aa](https://github.com/agentscope-ai/HiClaw/commit/48ce4aa))
- fix(controller): add Human reconcile exponential backoff and observedGeneration parity ([48ce4aa](https://github.com/agentscope-ai/HiClaw/commit/48ce4aa))
- fix(install): add non-interactive deep-defense guards to step functions ([6cbec18](https://github.com/agentscope-ai/HiClaw/commit/6cbec18))
- chore(helm): bump chart to 1.1.1 and update repo URLs ([fd09d98](https://github.com/agentscope-ai/HiClaw/commit/fd09d98))
- fix(install): update GitHub repo URL to agentscope-ai/HiClaw and bump stable fallback to v1.1.1 ([f39601a](https://github.com/agentscope-ai/HiClaw/commit/f39601a))
- fix(helm): clean up Manager/Worker pods on helm uninstall ([6570402](https://github.com/agentscope-ai/HiClaw/commit/6570402))
- fix(manager): align container-api.sh paths with controller /api/v1 ([5c9a653](https://github.com/agentscope-ai/HiClaw/commit/5c9a653))
- feat(install): swap runtime selection order to make QwenPaw the default ([d3e33e8](https://github.com/agentscope-ai/HiClaw/commit/d3e33e8))
- feat(install): support keep-all upgrade mode and enter-to-keep for all params ([c9ab98f](https://github.com/agentscope-ai/HiClaw/commit/c9ab98f))
- fix(agent): use roomID when parsing hiclaw get workers JSON output ([efcb544](https://github.com/agentscope-ai/HiClaw/commit/efcb544))
- fix(install): make error() multi-line safe by splitting exit into die() ([e21ac83](https://github.com/agentscope-ai/HiClaw/commit/e21ac83))
- fix(install): retry on too-short admin password instead of exiting ([19777eb](https://github.com/agentscope-ai/HiClaw/commit/19777eb))
- fix(auth): cap and sweep TokenReview cache ([2991d06](https://github.com/agentscope-ai/HiClaw/commit/2991d06))
- chore(controller): graceful shutdown for HTTP server and background goroutines ([fc99788](https://github.com/agentscope-ai/HiClaw/commit/fc99788))
- feat(controller): export per-CRD reconcile metrics ([5d7e721](https://github.com/agentscope-ai/HiClaw/commit/5d7e721))
- fix(legacy): preserve user plugin customizations on Manager config push ([f07a32f](https://github.com/agentscope-ai/HiClaw/commit/f07a32f))
- fix(helm): disable Tuwunel default displayname suffix ([ab5cdcf](https://github.com/agentscope-ai/HiClaw/commit/ab5cdcf))
- feat(controller): support Nacos remote skills with STS auth ([fb01fe6](https://github.com/agentscope-ai/HiClaw/commit/fb01fe6))
- fix(bootstrap): propagate observability and stream timeout env ([df98989](https://github.com/agentscope-ai/HiClaw/commit/df98989))
- fix(agent): quote coding CLI skill frontmatter ([bd11844](https://github.com/agentscope-ai/HiClaw/commit/bd11844))
- feat(install): optimize container runtime socket detection for rootless podman ([b1f103b](https://github.com/agentscope-ai/HiClaw/commit/b1f103b))
- fix(copaw): stop typing indicator on empty completion ([78418b5](https://github.com/agentscope-ai/HiClaw/commit/78418b5))
- fix(copaw): use display name instead of MXID in mention body ([02ff138](https://github.com/agentscope-ai/HiClaw/commit/02ff138))
- fix(controller): preserve runtime package files on reconcile ([8cb9f46](https://github.com/agentscope-ai/HiClaw/commit/8cb9f46))
- feat(copaw): make ReAct max iterations configurable ([933a600](https://github.com/agentscope-ai/HiClaw/commit/933a600))
- feat(controller): separate CR names from runtime worker names ([12da1ce](https://github.com/agentscope-ai/HiClaw/commit/12da1ce))
- fix(copaw): require slash-prefixed control commands ([e94aceb](https://github.com/agentscope-ai/HiClaw/commit/e94aceb))
- feat(agent): prohibit direct credential file access ([046537b](https://github.com/agentscope-ai/HiClaw/commit/046537b))
- fix(manager): hot-reload groupAllowFrom when Workers are created ([94bde15](https://github.com/agentscope-ai/HiClaw/commit/94bde15))
- fix(copaw): seed worker heartbeat interval ([ec0f57d](https://github.com/agentscope-ai/HiClaw/commit/ec0f57d))
- fix(copaw): align install dir with worker home ([c0bca77](https://github.com/agentscope-ai/HiClaw/commit/c0bca77))
- fix(copaw): exclude inbound thread messages from room history ([8d6a852](https://github.com/agentscope-ai/HiClaw/commit/8d6a852))
- fix(copaw): skip mc alias setup in k8s mode ([fc1b934](https://github.com/agentscope-ai/HiClaw/commit/fc1b934))
- fix(controller): preserve default object-storage access entries ([a940d94](https://github.com/agentscope-ai/HiClaw/commit/a940d94))
- fix(copaw): suppress missing MinIO object warnings ([53d270e](https://github.com/agentscope-ai/HiClaw/commit/53d270e))
- feat(controller): propagate skills API defaults to workers ([e4a3506](https://github.com/agentscope-ai/HiClaw/commit/e4a3506))
- feat(team-leader): refresh coordination builtins ([bfd99cd](https://github.com/agentscope-ai/HiClaw/commit/bfd99cd))
- fix(controller): apply Higress stream idle timeout ([8d81c9f](https://github.com/agentscope-ai/HiClaw/commit/8d81c9f))
- feat(controller): support team human coordinators ([16e87c2](https://github.com/agentscope-ai/HiClaw/commit/16e87c2))
- feat(copaw): add runtime coordination tools ([4a2ced6](https://github.com/agentscope-ai/HiClaw/commit/4a2ced6))
- fix(install): pass stream idle timeout on Windows ([fece949](https://github.com/agentscope-ai/HiClaw/commit/fece949))
- refactor(team-leader): remove legacy skill aliases ([67a6daf](https://github.com/agentscope-ai/HiClaw/commit/67a6daf))
- fix(team-leader): mirror worker's anti-loop reply rules ([2a7cd17](https://github.com/agentscope-ai/HiClaw/commit/2a7cd17))

- `c15ba378` fix(controller): join Team members to Team Room (#1149)
- `c2d84b09` fix(skills): complete manager and worker skill loading (#1153)
- `90f861c0` test(skills): verify Manager-to-Worker distribution (#1156)

**Also in this window / 闁告艾鏈﹢锟犲礂閺堢數閾傞柛娆惿戝ú?*

- Archive the v1.2.1 changelog before collecting the v1.2.2 release window. ([#1146](https://github.com/agentscope-ai/AgentTeams/pull/1146))
- Add integration coverage for Manager-to-Worker Skill distribution through `Worker.spec.skills`, Worker storage, and the QwenPaw Skill API. ([#1156](https://github.com/agentscope-ai/AgentTeams/pull/1156))

- **Worker static S3 credentials (endpoint + AK/SK)**: `worker-entrypoint.sh` Step 1 now configures the mc alias from `AGENTTEAMS_FS_ENDPOINT/ACCESS_KEY/SECRET_KEY` first — the same behavior as the Manager container — so `provider=oss` deployments with long-lived static credentials work without controller-mediated STS. Controller STS remains the fallback when no static credentials are injected.

- **Team worker room Manager removal (kick 403 fix)**: worker rooms now grant the Manager power level 50 (admin keeps 100), and when removing the Manager from a team-worker personal room fails with a kick 403 (same power level), `WorkerReconciler` falls back to `Provisioner.LeaveManagerRoom` — the Manager leaves with its own token, avoiding the Matrix power-level kick restriction entirely.

- **Leftover-room provisioning fix**: `ProvisionWorker` now treats a 403 "already in the room" from CreateRoom (leftover room whose alias still resolves, admin already a member) as an existing-room resolution — it resolves the alias to its room ID and continues provisioning the gateway consumer and downstream phases, instead of failing the whole provision (which previously left the worker with an empty roomID and no consumer, causing 401 on its first LLM call).

- **Synapse media download 404 fix (authenticated media)**: Synapse ≥ 1.121 marks uploaded media as `authenticated` (MSC4016) by default (`enable_authenticated_media` defaults to true), and the `/ _matrix/media/v3/download` `DownloadResource` calls `get_local_media(..., allow_authenticated=False)`, so a token-less download (Element Web attachment URLs carry only `allow_redirect=true`) raises `NotFoundError` → 404 even when the DB record and local file are intact. The Helm chart now sets `matrix.synapse.enableAuthenticatedMedia: false` by default (private clusters have no federation, so authenticated media is pointless), restoring attachment downloads.

- **Team/Human reconcile tuning (backport from `dev`)**: re-wire the tuning knobs onto the `workerMembers`-based Team reconciler so a slow/failing Team can no longer starve the workqueue:
  - `AGENTTEAMS_TEAM_MAX_CONCURRENT_RECONCILES` (default 1) raises Team reconcile parallelism above the controller-runtime default of 1.
  - `AGENTTEAMS_TEAM_RECONCILE_TIMEOUT_SECONDS` (default 0 = off) bounds a single reconcile pass so a hung external dependency fails fast instead of holding the worker slot.
  - `AGENTTEAMS_TEAM_RECONCILE_INTERVAL_SECONDS` (default 300) sets the periodic requeue for a fully converged Active Team, plus 0–10% positive jitter so Teams do not wake in lockstep.
  - `AGENTTEAMS_TEAM_ACTIVE_NO_REQUEUE` (default false) stops the periodic requeue for converged Active Teams; they reconcile only on events.
  - `failTeam` now applies exponential backoff (30s → 1m → … capped 10m) and stops requeuing after 5 consecutive failures (`status.maxRetriesReached=true`), re-armed via `kubectl annotate team <name> agentteams.io/retry=""`; the Human reconciler shares the same backoff via `failHuman` and the same annotation.
  - `TeamStatus.ObservedGeneration` is recorded on success and used to short-circuit unchanged Active teams on restart/informer re-sync (fast path), and `Reconcile` gains team-scoped logging (`team=`/`teamUID=`), a panic guard, per-step `team reconcile: step N` timing logs, and an optional per-pass deadline.
